clear
close all
clc

A = 2;
B = 2;
C = 3;
D = 3;

[x y] = meshgrid(-1.5:0.1:1.5, -1.5:0.1:1.5);

z = -x -y;

surf(x, y, z)
hold on
plot3(1, 1, 1, "o")
hold on
plot3(-1, -1, -1, "o")

fun = @(x,y,z) 1/(2*pi)*exp(-((x-1)^2 + (y-1)^2 + (z-1)^2)/2);

q = integral3(fun, )